# fluendo_test_zstddec
Fluendo test decoder plugin for GStreamer

Under scripts folder, you can find initial_setup_ubuntu.sh. It is a script which install all the dependencies for Ubuntu 22/24, prior to the cloning and compilation.